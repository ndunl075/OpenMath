{
  "name": "openmath",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r --filter=./packages/* build && pnpm --filter=@openmath/web build",
    "test": "vitest run",
    "typecheck": "tsc -b --force",
    "dev": "pnpm --filter=@openmath/web dev"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
